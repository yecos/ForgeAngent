# Forge Python Backend — Requirements
# Tested with Python 3.10+

# Web framework + WebSocket
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-socketio==5.12.1

# Local LLM via Ollama
ollama==0.4.7

# Multi-agent orchestration
crewai==0.95.0
crewai-tools==0.25.8

# Vector database (persistent memory + RAG)
chromadb==0.6.3

# Document parsing for RAG
pypdf==5.1.0
python-docx==1.1.2
markdown==3.7

# Web search (no API key required)
duckduckgo-search==7.3.0

# Embeddings (local, runs on CPU or GPU)
sentence-transformers==3.3.1

# Utilities
pydantic==2.10.4
python-multipart==0.0.20
aiofiles==24.1.0
