# Forge Python Backend — Requirements
# Tested with Python 3.10+ on Linux, macOS and Windows
#
# NOTE: CrewAI has been removed because:
#   1. It pins specific versions of pydantic/openai/langchain that conflict
#      with chromadb and other deps (ResolutionImpossible errors on Windows).
#   2. It was actually dead code — our run_orchestration() does the multi-agent
#      loop manually with Ollama chat completions (faster, simpler, no deps).
#
# If you really want CrewAI later, install it separately:
#   pip install crewai crewai-tools --no-deps
# and resolve the conflicts manually.

# ── Web framework + WebSocket ──────────────────────────────────────────
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
python-socketio>=5.10,<6.0
python-multipart>=0.0.7
aiofiles>=23.0

# ── Local LLM via Ollama ───────────────────────────────────────────────
ollama>=0.4,<1.0

# ── Vector database (memory + RAG) ─────────────────────────────────────
chromadb>=0.5,<1.0

# ── Web search (no API key required) ───────────────────────────────────
duckduckgo-search>=6.0,<8.0

# ── Utilities ──────────────────────────────────────────────────────────
pydantic>=2.5,<3.0
